---
import NotesLayout from "../../../layouts/NotesLayout.astro";
import { sitePath } from "../../../lib/site.js";
---

<NotesLayout
  title="2026 Agent 技术全景 | 技术笔记"
  description="从 ReAct 到 Agent Runtime、MCP 与可治理智能体系统，梳理 2026 年 AI Agent 工程的关键技术主线。"
>
  <article class="article-page prose">
    <header class="article-header">
      <p class="eyebrow">Agent 工程</p>
      <h1>2026 Agent 技术全景：从 ReAct 到 Agent Runtime、MCP 与可治理智能体系统</h1>
      <p>
        截至 2026 年 7 月 19 日，Agent 的竞争重点已经不再只是“模型能否调用工具”，
        而是系统能否在长任务中持续运行、正确恢复、接受监督，并安全地完成真实操作。
      </p>
      <div class="article-meta"><span>Agent Engineering</span><span>约 25 分钟阅读</span></div>
    </header>

    <h2>一、什么是 AI Agent？</h2>
    <p>传统大语言模型通常采用一次输入、一次输出的工作方式：</p>
    <pre class="flow-code"><code>用户问题 → 大语言模型 → 文本答案</code></pre>
    <p>而 Agent 会围绕目标持续执行“思考、行动、观察、修正”循环：</p>
    <pre class="flow-code"><code>用户目标
   ↓
分析与规划
   ↓
调用工具
   ↓
观察执行结果
   ↓
判断是否完成
   ├── 否：修正方案并继续执行
   └── 是：输出最终结果</code></pre>
    <p>
      因此，Agent 不只是一个聊天机器人，而是一个由大语言模型驱动、
      能够使用工具并对执行结果进行反馈控制的软件系统。
      现代 Agent 通常需要规划、工具调用、状态维护、协作和多步骤任务执行能力。
    </p>

    <h2>二、Agent 技术经历了怎样的演进？</h2>
    <p>Agent 技术大致经历了四个阶段：Prompt Agent、Tool-Calling Agent、Workflow Agent 和 Long-Horizon Agent。</p>

    <figure class="article-figure wide">
      <img src={sitePath("notes/agent-landscape/agent-evolution-roadmap.png")} alt="Agent 技术演进路线图" loading="lazy" />
      <figcaption>Agent 从只会回答的 Prompt Agent，逐步演进为能够调用工具、管理流程并长期自主执行的完整系统。</figcaption>
    </figure>

    <h3>1. Prompt Agent</h3>
    <p>最早期的 Agent 主要依靠复杂 Prompt，让模型按照指定步骤输出结果。</p>
    <pre class="flow-code"><code>Prompt + LLM → Response</code></pre>
    <p>这种方式实现简单，但存在状态不可控、错误无法恢复和工具能力有限等问题。</p>

    <h3>2. Tool-Calling Agent</h3>
    <p>模型开始根据任务动态调用搜索、数据库、代码执行和外部 API。</p>
    <pre class="flow-code"><code>LLM → Tool Call → Tool Result → LLM</code></pre>
    <p>ReAct、Function Calling 和 Tool Use 都属于这一阶段的代表技术。</p>

    <h3>3. Workflow Agent</h3>
    <p>开发者通过状态图、条件路由和固定节点约束 Agent 的执行过程。</p>
    <pre class="flow-code"><code>输入
 ↓
任务分析
 ↓
工具执行
 ↓
结果验证
 ↓
失败重试或成功返回</code></pre>
    <p>
      LangGraph 等框架开始承担工作流编排、状态持久化、Human-in-the-loop
      和故障恢复等职责。它更接近“可恢复的运行时”，而不是只负责包一层模型调用。
    </p>

    <h3>4. Long-Horizon Agent</h3>
    <p>
      2026 年的重点正在转向可以执行分钟级、小时级甚至更长任务的 Agent。
      这类 Agent 不仅需要模型能力，还需要可持久化任务状态、受控执行环境、上下文压缩、
      长期记忆、失败恢复、断点续跑、权限控制、人工审批、Trace 和评测系统。
    </p>

    <h2>三、Agent 已经从 Prompt 范式变成完整的软件系统</h2>
    <p>早期 Agent 通常可以概括为：</p>
    <pre class="flow-code"><code>LLM + Prompt + ReAct + Function Calling</code></pre>
    <p>
      这种结构能够完成简单搜索、问答和工具调用，但在任务步骤增加后，容易出现上下文膨胀、
      重复调用、状态丢失、错误累积和无法恢复等问题。现在更完整的 Agent 系统通常包含：
    </p>
    <pre class="flow-code"><code>Model
+ Agent Harness / Runtime
+ Workflow
+ Tools
+ Memory
+ Sandbox
+ Protocols
+ Observability
+ Evaluation
+ Security Governance</code></pre>
    <p>
      模型负责理解、推理和决策，运行时负责把决策放进可恢复、可审计、受权限约束的执行环境。
      因此，评价一个 Agent 项目时，不能只看模型和 Prompt，还要看完整的软件工程系统。
    </p>

    <h2>四、2026 年 Agent 的七个核心技术趋势</h2>
    <h3>1. 从自由循环转向 Workflow-first</h3>
    <p>传统 ReAct Agent 通常由模型自行决定下一步行动：</p>
    <pre class="flow-code"><code>Thought → Action → Observation → Thought</code></pre>
    <p>
      这种方式灵活，但执行路径难以预测。生产级系统更适合采用“确定性工作流 + 局部智能决策”的混合模式：
    </p>
    <pre class="flow-code"><code>输入
 ↓
任务分类与确定性路由
 ↓
Agent 推理与工具调用
 ↓
规则校验
 ↓
人工审批或自动提交</code></pre>
    <p>
      LangGraph 将 Agent 建模为有状态图，通过 Checkpoint 支持持久化、故障恢复和 Human-in-the-loop；
      Microsoft Agent Framework 也将工作流显式建模为执行器与边，并提供 Checkpoint 机制。
      新的工程原则是：<strong>让模型负责不确定的判断，让工作流负责确定的执行边界。</strong>
    </p>

    <h3>2. Agent Harness / Runtime 成为核心基础设施</h3>
    <p>
      Agent Harness 可以理解为包裹在模型外部的运行时系统。
      模型负责分析、规划和决策，Harness 负责执行控制、状态、权限、安全、日志与评测。
    </p>

    <figure class="article-figure wide">
      <img src={sitePath("notes/agent-landscape/agent-harness-architecture.png")} alt="Agent Harness 架构图" loading="lazy" />
      <figcaption>模型负责推理，Harness 负责工具执行、状态保存、失败恢复、权限控制和人工审批。</figcaption>
    </figure>

    <pre class="flow-code"><code>Agent Harness
├── System Instructions
├── Tool Registry
├── Workspace
├── Sandbox
├── State Management
├── Retry Policy
├── Permission Control
├── Human Approval
├── Trace Logging
└── Evaluation</code></pre>
    <p>一个生产级 Harness 通常需要回答这些问题：</p>
    <ul>
      <li>Agent 可以访问哪些文件？</li>
      <li>哪些工具允许自动调用？</li>
      <li>删除文件、发邮件或执行命令是否需要确认？</li>
      <li>工具执行失败后如何重试？</li>
      <li>任务如何暂停和恢复？</li>
      <li>如何防止 Agent 陷入无限循环？</li>
      <li>如何判断最终结果是否合格？</li>
    </ul>
    <p>
      OpenAI Agents SDK 已将 Sessions、Tracing、Guardrails、Handoff 和 Human-in-the-loop
      作为运行时能力。高风险工具可以暂停执行，待人工批准后再从持久化状态继续。
    </p>

    <h3>3. Agent 协议栈正在形成</h3>
    <p>当前 Agent 系统逐渐形成三层协议分工：</p>

    <h4>MCP：Agent 与工具和数据</h4>
    <p>
      Model Context Protocol 统一模型应用访问外部数据、资源和工具的方式。
      数据库、GitHub、文件系统、浏览器和企业 API 可以封装为 MCP Server，由不同 Agent 客户端调用。
    </p>
    <pre class="flow-code"><code>Agent / MCP Client
        ↓
    MCP Protocol
        ↓
MCP Server
├── Tools
├── Resources
└── Prompts</code></pre>

    <h4>A2A：Agent 与 Agent</h4>
    <p>
      Agent2Agent Protocol 面向不同框架、服务或组织中的 Agent，支持能力发现、消息交换、
      任务委派、流式状态更新和结果返回。它解决的是跨 Agent 的互操作与协作问题。
    </p>

    <h4>AG-UI：Agent 与用户界面</h4>
    <p>
      AG-UI 是连接 Agent 后端与用户界面的事件协议，可传递流式消息、工具调用状态、
      共享状态、执行进度和 Human-in-the-loop 交互。
    </p>
    <pre class="flow-code"><code>AG-UI：Agent ↔ 用户界面
A2A：  Agent ↔ Agent
MCP：  Agent ↔ 工具与数据</code></pre>

    <figure class="article-figure wide">
      <img src={sitePath("notes/agent-landscape/agent-protocol-stack.png")} alt="2026 Agent 协议栈" loading="lazy" />
      <figcaption>AG-UI 连接用户界面，A2A 组织 Agent 协作，MCP 连接工具与数据。</figcaption>
    </figure>
    <p>
      协议提供的是标准化接口，不等于完整的任务编排。MCP 不负责规划，A2A 也不会自动解决多 Agent
      的状态一致性、权限和结果验证问题。
    </p>

    <h3>4. Prompt Engineering 正在升级为 Context Engineering</h3>
    <p>
      Prompt Engineering 关注“如何写好一条提示词”，而 Context Engineering 关注：
      在每一个执行步骤中，应该向模型提供哪些信息。
    </p>
    <p>
      Agent 的上下文可能包括当前用户目标、已完成任务、下一步计划、工具调用结果、历史错误、
      项目代码、长期记忆、安全规则和剩余 Token。把所有内容都塞进上下文会造成成本增加、
      注意力分散和关键信息丢失。
    </p>
    <p>
      现代 Agent 通常采用 Compaction、Retrieval、Note Taking、Context Isolation 和渐进式披露，
      让模型只在需要时获取相关信息。
    </p>

    <figure class="article-figure wide">
      <img src={sitePath("notes/agent-landscape/context-engineering.png")} alt="从 Prompt Engineering 到 Context Engineering" loading="lazy" />
      <figcaption>Context Engineering 的重点不是提供更多信息，而是筛选并按需加载当前真正需要的信息。</figcaption>
    </figure>

    <h4>Skills：将能力封装为可复用资源</h4>
    <p>
      早期 Agent 常把所有规则、案例和操作步骤写进 System Prompt。随着任务复杂度增加，
      更合理的方式是把特定能力封装为可版本管理、可独立测试的 Skill：
    </p>
    <pre class="flow-code"><code>基础指令
+ AGENTS.md 项目规范
+ 按需加载的 Skills
+ 动态检索的上下文
+ 当前任务状态</code></pre>
    <pre class="flow-code"><code>skills/
├── github-pr-review/
│   ├── SKILL.md
│   ├── templates/
│   └── scripts/
├── data-analysis/
│   ├── SKILL.md
│   └── examples/
└── release-check/
    ├── SKILL.md
    └── checklist.md</code></pre>
    <p>Tool 解决“能不能做”，Skill 更接近可复用的“如何稳定做好”。</p>

    <h3>5. Memory 正在从聊天记录升级为时间化知识系统</h3>
    <p>Memory 不等于简单保存历史聊天。生产级 Agent 通常需要区分多种记忆：</p>

    <figure class="article-figure wide">
      <img src={sitePath("notes/agent-landscape/agent-memory-layers.png")} alt="Agent Memory 五层记忆系统" loading="lazy" />
      <figcaption>记忆需要可写入、可检索、可更新和可遗忘，而不是无限堆积聊天记录。</figcaption>
    </figure>

    <table>
      <thead>
        <tr><th>记忆类型</th><th>主要作用</th></tr>
      </thead>
      <tbody>
        <tr><td>Working Memory</td><td>保存当前任务的临时状态</td></tr>
        <tr><td>Session Memory</td><td>保存当前会话的历史与摘要</td></tr>
        <tr><td>Episodic Memory</td><td>记录过去发生过的任务和结果</td></tr>
        <tr><td>Semantic Memory</td><td>保存稳定的事实、规则和用户偏好</td></tr>
        <tr><td>Procedural Memory</td><td>保存完成某类任务的操作流程</td></tr>
        <tr><td>Artifact Memory</td><td>通过文件、代码和报告保存外部状态</td></tr>
      </tbody>
    </table>
    <p>
      一个完整的 Memory 系统还需要处理记忆提取、去重合并、冲突检测、来源追踪、
      置信度、时间衰减、权限隐私和用户删除记忆。
    </p>
    <p>
      代表性路线包括强调有状态 Agent 的 Letta、作为通用记忆层的 Mem0，以及使用时间上下文图保存实体、
      关系、事实有效期和来源的 Graphiti。未来的 Agent Memory 不只是向量检索，还要能够处理事实冲突、
      时间有效性、写入者身份、遗忘策略和来源追溯。
    </p>

    <h3>6. Agent Evaluation 正在取代“感觉效果不错”</h3>
    <p>
      普通 LLM 评测通常只检查最终回答是否正确。但 Agent 的最终结果可能正确，
      执行过程却存在严重问题，例如调用了错误工具、执行了危险操作、使用过多 Token、
      进行了大量无效搜索，或者偶然得到了正确答案。
    </p>

    <figure class="article-figure wide">
      <img src={sitePath("notes/agent-landscape/agent-evaluation-loop.png")} alt="Agent Evaluation 数据驱动改进闭环" loading="lazy" />
      <figcaption>通过 Trace、失败案例、Dataset 和 Grader，把主观体验转化为可重复的评测与改进闭环。</figcaption>
    </figure>

    <p>因此，Agent 评测需要同时覆盖：</p>
    <ul>
      <li><strong>Result Evaluation：</strong>最终结果是否正确。</li>
      <li><strong>Trajectory Evaluation：</strong>工具选择、调用顺序和中间决策是否合理。</li>
      <li><strong>Safety Evaluation：</strong>是否执行越权、危险或不可逆操作。</li>
      <li><strong>Efficiency Evaluation：</strong>Token、延迟、工具调用次数和计算成本是否合理。</li>
      <li><strong>Robustness Evaluation：</strong>在工具失败、网络异常和输入变化时是否仍能完成任务。</li>
    </ul>
    <table>
      <thead>
        <tr><th>指标</th><th>关注的问题</th></tr>
      </thead>
      <tbody>
        <tr><td>Task Success Rate</td><td>最终任务是否完成</td></tr>
        <tr><td>Tool / Argument Accuracy</td><td>工具与参数是否正确</td></tr>
        <tr><td>Trajectory Quality</td><td>执行路径是否合理</td></tr>
        <tr><td>Recovery Rate</td><td>工具失败后能否恢复</td></tr>
        <tr><td>Human Intervention Rate</td><td>多少任务需要人工接管</td></tr>
        <tr><td>Cost / Latency</td><td>完整任务的成本与耗时</td></tr>
        <tr><td>Safety Violation Rate</td><td>是否触发越权或危险操作</td></tr>
      </tbody>
    </table>
    <pre class="flow-code"><code>运行
→ 收集 Trace
→ 分析失败案例
→ 构建 Eval
→ 修改 Prompt / Tool / Workflow / Skill
→ 回归测试
→ 灰度发布</code></pre>

    <h3>7. 安全重点从内容过滤转向能力约束</h3>
    <p>
      当 Agent 可以发送邮件、修改代码、执行 Shell、访问数据库或调用企业系统时，
      一次错误决策就可能产生真实副作用。主要风险包括间接 Prompt Injection、权限过大、
      敏感信息泄露、恶意 MCP Server、Skill 供应链攻击、长期记忆投毒、命令注入和未经批准的写操作。
    </p>

    <figure class="article-figure wide">
      <img src={sitePath("notes/agent-landscape/agent-safety-boundaries.png")} alt="Agent 三层安全边界" loading="lazy" />
      <figcaption>通过输入防护、工具权限与执行安全三层边界，实现默认拒绝、按需授权和全程审计。</figcaption>
    </figure>

    <p>比单纯识别恶意文本更重要的是约束 Agent 的实际能力边界：</p>
    <pre class="flow-code"><code>默认最小权限
      ↓
读工具与写工具分离
      ↓
敏感参数结构化校验
      ↓
命令在沙箱中执行
      ↓
删除、发送、付款等操作必须审批
      ↓
记录完整审计 Trace</code></pre>
    <p>
      安全设计的目标不是假设 Agent 永远不会被误导，而是即使发生错误，也能限制影响范围，
      阻止未授权副作用，并保留可审计的执行证据。
    </p>

    <h2>五、2026 年推荐的 Agent 系统架构</h2>
    <p>一个较完整的生产级 Agent 可以采用以下架构：</p>

    <figure class="article-figure wide">
      <img src={sitePath("notes/agent-landscape/production-agent-architecture.png")} alt="2026 生产级 Agent 技术栈" loading="lazy" />
      <figcaption>生产级 Agent 由前端、API Gateway、Workflow Runtime、多 Agent、工具与记忆、安全和评测共同组成。</figcaption>
    </figure>

    <pre class="flow-code"><code>用户目标
  ↓
Web / Mobile UI
  ↕ AG-UI / SSE
API Gateway
  ↓
Agent Workflow Runtime / Harness
  ├── Router / Planner
  ├── Research Agent ─┐
  ├── Code Agent      ├─ A2A
  └── Review Agent   ─┘
  ├── MCP Client → Files / GitHub / Database / Browser
  ├── Memory → Session / Vector Store / Temporal Graph
  ├── Sandbox
  ├── Human Approval
  └── Tracing → Datasets / Graders / Evals
  ↓
最终结果</code></pre>
    <p>
      Workflow Runtime 管理状态、路由和恢复；MCP 管理外部工具连接；A2A 管理跨服务 Agent 协作；
      AG-UI 管理前端流式交互；Memory 管理跨会话信息；Sandbox 与 Human Approval 控制高风险副作用；
      Trace 与 Eval 构成持续改进闭环。
    </p>

    <h2>六、多 Agent 是否一定比单 Agent 更好？</h2>
    <p>答案是否定的。多 Agent 会引入额外成本：</p>
    <ul>
      <li>Token 消耗增加；</li>
      <li>通信延迟增加；</li>
      <li>状态同步更加复杂；</li>
      <li>错误来源增多；</li>
      <li>调试难度提升；</li>
      <li>多个 Agent 可能重复工作。</li>
    </ul>
    <p>
      只有当任务可以有效并行，或者不同子任务需要明显不同的专业能力时，多 Agent 才更有价值。
      对于流程明确、步骤较少的任务，一个带工具和状态机的单 Agent 往往更加稳定。
    </p>

    <h2>七、值得关注的 GitHub 项目</h2>
    <h3>Agent 编排框架</h3>
    <table>
      <thead>
        <tr><th>项目</th><th>主要特点</th><th>适合场景</th></tr>
      </thead>
      <tbody>
        <tr><td>OpenAI Agents SDK</td><td>Handoff、Guardrail、Session、Tracing</td><td>轻量 Agent 与多步骤执行</td></tr>
        <tr><td>LangGraph</td><td>状态图、Checkpoint、持久化、可恢复执行</td><td>复杂且需要精确控制的工作流</td></tr>
        <tr><td>Google ADK</td><td>Agent 与顺序、并行、循环工作流</td><td>Google / Gemini 生态</td></tr>
        <tr><td>Microsoft Agent Framework</td><td>工作流、Checkpoint、企业集成</td><td>微软生态和企业部署</td></tr>
        <tr><td>CrewAI</td><td>Role-based Crews 与事件驱动 Flows</td><td>角色化多 Agent 原型</td></tr>
        <tr><td>Pydantic AI</td><td>类型安全、结构化输出、模型无关</td><td>Python 后端与强数据约束应用</td></tr>
        <tr><td>smolagents</td><td>轻量 Code Agent 与沙箱执行</td><td>教学、研究和轻量代码 Agent</td></tr>
      </tbody>
    </table>

    <h3>Agent 应用、协议与记忆</h3>
    <ul>
      <li><strong>OpenHands、SWE-agent：</strong>真实代码仓库中的分析、修改和验证。</li>
      <li><strong>Browser Use：</strong>通过浏览器完成点击、输入、表单和数据提取。</li>
      <li><strong>MCP、A2A、AG-UI：</strong>分别连接工具与数据、其他 Agent 和用户界面。</li>
      <li><strong>Letta、Mem0、Graphiti：</strong>有状态 Agent、通用记忆层和时间化知识图谱。</li>
    </ul>

    <h2>八、实际项目应该如何选型</h2>
    <h3>个人知识库、岗位匹配与数据分析</h3>
    <pre class="flow-code"><code>React / Vue
+ FastAPI
+ OpenAI Agents SDK 或 Pydantic AI
+ PostgreSQL / Redis
+ Qdrant / Chroma
+ MCP
+ OpenTelemetry
+ Agent Evals</code></pre>
    <p>如果项目包含复杂分支、失败重试、长期状态和人工审批，可将编排层替换为 LangGraph 或其他工作流运行时。</p>

    <h3>需要明确专业分工的多 Agent</h3>
    <pre class="flow-code"><code>Research Agent：收集证据
Analysis Agent：分析数据
Writer Agent：生成报告
Reviewer Agent：检查事实和格式</code></pre>
    <p>
      只有当任务可以拆分为相对独立的专业角色时，才值得采用 Multi-Agent。
      如果多个 Agent 使用相同模型、上下文和工具，增加 Agent 数量通常只会提高 Token 消耗和调试难度。
    </p>

    <h2>九、推荐学习路线</h2>
    <ol>
      <li><strong>掌握基本 Agent Loop：</strong>Function Calling、Tool Schema、ReAct、Structured Output、Pydantic 和错误重试。</li>
      <li><strong>学习状态化编排：</strong>LangGraph State、Node、Edge、Conditional Edge、Checkpoint、Human-in-the-loop 和 Streaming。</li>
      <li><strong>加入 Memory 和 RAG：</strong>短期状态、长期记忆、向量检索、混合检索、记忆冲突和 Context Compaction。</li>
      <li><strong>掌握 MCP 和 Skills：</strong>MCP Client、MCP Server、Tool、Resource、Prompt、Skills 封装和权限控制。</li>
      <li><strong>构建生产级 Harness：</strong>Docker Sandbox、最小权限、审批机制、Trace、超时重试、任务恢复、Agent Evals 和成本监控。</li>
    </ol>

    <h2>十、总结</h2>
    <p>2026 年 Agent 技术的核心变化，可以概括为一句话：</p>
    <blockquote>
      <p>Agent 正在从“会调用工具的语言模型”，演化为“具有运行时、协议、记忆、权限、评测和恢复能力的软件系统”。</p>
    </blockquote>
    <p>未来真正决定 Agent 能否落地的，不只是模型参数和推理能力，而是整套 Agent Engineering 能力：</p>
    <pre class="flow-code"><code>Model
+ Harness
+ Context
+ Memory
+ Skills
+ MCP
+ Protocols
+ Sandbox
+ Trace
+ Evaluation
+ Security Governance</code></pre>
    <p>对于开发者而言，最值得掌握的是以下五项底层能力：</p>
    <ol>
      <li>使用状态机控制 Agent 的执行过程；</li>
      <li>使用 Harness 管理长任务和失败恢复；</li>
      <li>使用 MCP 标准化工具接入；</li>
      <li>使用 Memory 和 Context Engineering 管理信息；</li>
      <li>使用 Trace、Verifier 和 Evals 持续提升系统可靠性。</li>
    </ol>
    <p>
      Agent 的下一个阶段，不是让模型“看起来更聪明”，而是让整个系统真正做到：
      <strong>能执行、能恢复、能验证、能协作，也能被安全地控制。</strong>
    </p>
    <p>
      模型决定 Agent 的能力上限，而 Harness Engineering、Context Engineering 和 Evaluation Engineering，
      决定它能否真正进入生产环境。
    </p>

    <h2>参考资料</h2>
    <ul>
      <li><a href="https://openai.github.io/openai-agents-python/" target="_blank" rel="noreferrer">OpenAI Agents SDK documentation</a></li>
      <li><a href="https://openai.github.io/openai-agents-python/human_in_the_loop/" target="_blank" rel="noreferrer">OpenAI Agents SDK：Human-in-the-loop</a></li>
      <li><a href="https://docs.langchain.com/oss/python/langgraph/overview" target="_blank" rel="noreferrer">LangGraph documentation</a></li>
      <li><a href="https://docs.langchain.com/oss/python/langgraph/persistence" target="_blank" rel="noreferrer">LangGraph：Persistence and checkpoints</a></li>
      <li><a href="https://modelcontextprotocol.io/specification/latest" target="_blank" rel="noreferrer">Model Context Protocol specification</a></li>
      <li><a href="https://a2a-protocol.org/latest/specification/" target="_blank" rel="noreferrer">Agent2Agent Protocol specification</a></li>
      <li><a href="https://docs.ag-ui.com/" target="_blank" rel="noreferrer">AG-UI documentation</a></li>
      <li><a href="https://learn.microsoft.com/en-us/agent-framework/workflows/workflows" target="_blank" rel="noreferrer">Microsoft Agent Framework Workflows</a></li>
    </ul>

    <div class="article-next">
      <span>继续阅读</span>
      <a href={sitePath("notes/agent/context-engineering/")}>Context Engineering →</a>
    </div>
  </article>
</NotesLayout>
