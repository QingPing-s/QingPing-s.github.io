---
import NotesLayout from "../../../layouts/NotesLayout.astro";
import { sitePath } from "../../../lib/site.js";
---

<NotesLayout title="LLM-as-Judge 与 Rubric | 技术笔记" description="用清晰 Rubric 约束 LLM-as-Judge，使开放式 Agent 评测可复用、可校准。">
  <article class="article-page prose">
    <header class="article-header">
      <p class="eyebrow">Agent Evaluation</p>
      <h1>LLM-as-Judge 与 Rubric</h1>
      <p>
        LLM-as-Judge 适合评估开放式回答和复杂轨迹，但不能裸用。
        真正有价值的是：先设计 Rubric，再让 Judge 按稳定标准输出结构化评分。
      </p>
      <div class="article-meta"><span>Model-Graded Eval</span><span>约 8 分钟阅读</span></div>
    </header>

    <h2>LLM-as-Judge 适合评什么</h2>
    <ul>
      <li>答案是否满足用户意图。</li>
      <li>回答是否基于给定上下文，是否存在幻觉。</li>
      <li>输出是否遵守格式、语气、安全和业务约束。</li>
      <li>Agent 的工具调用路径是否合理。</li>
      <li>复杂任务是否部分完成，失败原因是什么。</li>
    </ul>

    <h2>Rubric 负责什么</h2>
    <p>
      Rubric 是一组明确评分标准，用来约束人类评审或 LLM Judge 的判断口径。
      没有 Rubric，Judge 很容易变成“另一个模型的主观印象”。
    </p>
    <ul>
      <li><strong>目标：</strong>本次任务应该完成什么。</li>
      <li><strong>输入：</strong>用户问题、上下文、工具返回、参考答案。</li>
      <li><strong>评分维度：</strong>正确性、完整性、证据、工具使用、安全边界、格式。</li>
      <li><strong>分数定义：</strong>明确 0、1、2、3 分分别代表什么。</li>
      <li><strong>失败条件：</strong>列出一票否决项，例如捏造事实、越权调用工具、泄露敏感信息。</li>
    </ul>

    <h2>基本流程</h2>
    <pre class="flow-code"><code>测试样本
  → 被测 Agent 输出 / Trace
  → Judge Prompt + Rubric
  → 结构化评分 JSON
  → 人工抽检与校准
  → 失败样本归因</code></pre>

    <h2>常见风险</h2>
    <ul>
      <li><strong>评审偏差：</strong>Judge 可能偏好长答案、熟悉风格或自身生成的表达。</li>
      <li><strong>不稳定：</strong>prompt、模型版本和温度变化都会影响分数。</li>
      <li><strong>解释不足：</strong>只给总分不够，需要输出理由和失败标签。</li>
      <li><strong>被表述迷惑：</strong>语言流畅不等于事实正确或任务完成。</li>
      <li><strong>不能替代人工：</strong>上线前仍需要人工抽检关键样本。</li>
    </ul>

    <h2>推荐输出格式</h2>
    <pre class="flow-code"><code>{`{
  "score": 0-3,
  "passed": true,
  "failure_tags": [],
  "reason": "...",
  "evidence": "...",
  "needs_human_review": false
}`}</code></pre>

    <h2>工程建议</h2>
    <p>
      Judge 输出要固定为 JSON 或有限选项，并保留原始输入、Agent 输出、评分理由、Rubric 版本和 Judge 模型版本。
      这样才能做回归分析，而不是只看一次性的平均分。
    </p>

    <h2>参考资料</h2>
    <ul>
      <li><a href="https://github.com/openai/evals/blob/main/docs/eval-templates.md" target="_blank" rel="noreferrer">OpenAI Evals: model-graded eval template</a></li>
      <li><a href="https://docs.evidentlyai.com/metrics/customize_llm_judge" target="_blank" rel="noreferrer">Evidently: Configure LLM Judges</a></li>
    </ul>

    <div class="article-next">
      <span>返回目录</span>
      <a href={sitePath("notes/")}>技术笔记 →</a>
    </div>
  </article>
</NotesLayout>
